#include <iostream>
#include "B1.h"

int main( int argc, char** argv ) {
	cout<<valEval(ck0(newIf(newApp(3, newVal(newPrim("<")), newVal(1), newVal(2)), newVal(1), newVal(2))));

	return 0;

}
